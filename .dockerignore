node_modules
.next
# Note: Railway strips .git from the build context regardless of what's in
# here, so we don't bother including it. The auto-bumping patch version is
# sourced from src/generated/commit-count.txt (maintained by pre-commit
# hook) when git isn't reachable inside the build container.
.git
.claude
.env
.env.local
.env.*.local
.cache
coverage
*.log
scripts/_probe-*
scripts/probe-*
scripts/*.bak
**/*.bak
.DS_Store
